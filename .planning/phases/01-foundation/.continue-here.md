---
context: phase
phase: 01-foundation
task: 6
total_tasks: 10
status: in_progress
last_updated: 2026-04-17T19:25:52.911Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint was discovered through failure this session.

- [ ] CONSTRAINT: FK+RLS on workspace creation — the POST /workspaces endpoint MUST use an explicit transaction with two SaveChangesAsync calls and SET LOCAL app.current_tenant between them. A single SaveChangesAsync for both workspace+member causes 23503 FK violation AND RLS block.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Single SaveChangesAsync for workspace+member | EF Core has no FK relationship configured between WorkspaceMember and Workspace in OnModelCreating, so insert order is undefined. Member insert before workspace → FK 23503. Even with correct order, RLS blocks because app.current_tenant is Guid.Empty during workspace creation. | blocking | WorkspaceEndpoints.cs now uses explicit tx + two saves + SET LOCAL (commit 0eae6f4). Do not revert to single save. |
| Live credentials in .env.example | AUTH_GITHUB_ID and AUTH_GITHUB_SECRET (real values) were committed. Git history contains them. | advisory | Rotate at github.com/settings/developers immediately. Scrub history with git filter-repo or BFG. |

<current_state>
Phase 01 (Foundation) — all 3 plans executed and human-verified E2E. Tests were 17/26 failing due to FK+RLS bug in POST /workspaces. Fix committed (0eae6f4). Tests NOT yet re-run to confirm. VERIFICATION.md created with status=human_needed pending test confirmation. Code review (01-REVIEW.md) found 4 critical and 6 warning issues.
</current_state>

<completed_work>

- Plan 01-01: Docker Compose stack + PostgreSQL schema with RLS, fractional-indexing, currency columns. Human-verified psql: non-superuser role, 5 tables, RLS isolation.
- Plan 01-02: .NET 9 backend — EF Core, TenantDbConnectionInterceptor, tenant middleware, JWT auth, WorkspaceEndpoints, MemberEndpoints, InviteEndpoints, SsoSyncEndpoints, BoardHub, xUnit+TestContainers test suite.
- Plan 01-03: Next.js 16 frontend — Auth.js v5 SSO (GitHub/Google/Microsoft Entra ID), workspace selector/creation, invite flow, member management UI. Human-verified all 11 E2E steps.
- Code review: 01-REVIEW.md committed (4 critical, 6 warning, 5 info).
- FK/RLS fix: WorkspaceEndpoints.cs split into explicit tx with two saves + SET LOCAL (commit 0eae6f4).
- PR branch: master-pr created from a5ffc6f with 5 code-only commits.
</completed_work>

<remaining_work>

**Immediate (must do before Phase 01 can be marked complete):**
1. Re-run: `cd api && dotnet test Nodefy.slnx --logger "console;verbosity=normal"` — expect 26/26 pass
2. If pass: update .planning/phases/01-foundation/01-VERIFICATION.md status from human_needed → passed
3. Mark Phase 01 complete: `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" phase complete "01"`
4. Commit tracking update

**Security (CRITICAL — do before pushing to GitHub):**
5. Rotate GitHub OAuth credentials at github.com/settings/developers
6. Scrub .env.example from git history (BFG or git filter-repo)

**Post-phase:**
7. Install gh CLI: `winget install --id GitHub.cli` then `gh auth login`
8. Push master-pr and create PR: `git checkout master-pr && /gsd-ship`
9. Consider /gsd-code-review-fix 1 to auto-fix remaining code review issues
</remaining_work>

<decisions_made>

- Workspace creation uses explicit transaction: save workspace → SET LOCAL app.current_tenant → save member → commit. Rationale: EF Core insert order undefined without FK nav property; RLS blocks member insert without tenant context.
- PR branch `master-pr` branched from a5ffc6f (last pre-execution commit) with only 5 code commits cherry-picked. No planning artifacts in PR diff.
- Auth.js v5 with GitHub /user/emails fallback confirmed working by human E2E verification.
</decisions_made>

<blockers>

- Tests not yet re-confirmed after FK fix — re-run required before phase completion
- Live GitHub credentials in .env.example (CR-01) — rotate immediately, scrub history before public push
- gh CLI not installed — needed for /gsd-ship
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-foundation/01-REVIEW.md` — 4 critical issues to address before Phase 2
2. `api/Nodefy.Api/Endpoints/WorkspaceEndpoints.cs` — understand the tx+SET LOCAL pattern before any modifications
3. `.planning/phases/01-foundation/01-VERIFICATION.md` — currently status=human_needed, update to passed after test run

## Infrastructure State
- Docker Compose: db service was started for human verification. May need `docker compose up -d db` before running tests.
- TestContainers: requires Docker Desktop running. Start Docker Desktop before `dotnet test`.
- master-pr branch: exists locally, NOT pushed to origin yet.
- .env: DB_PASSWORD=devpassword (local dev value, fine).

<context>
Session accomplished full Phase 01 execution: DB schema, .NET backend, Next.js frontend — all human-verified. Discovered and fixed a critical FK+RLS bug in workspace creation endpoint. Code review identified 4 critical issues (live credentials most urgent). Context hit 92% while confirming test fix, so pausing here. The fix is committed and correct — just needs test confirmation to close the loop.
</context>

<next_action>
Start with: `cd api && dotnet test Nodefy.slnx --logger "console;verbosity=normal"` to confirm 26/26 pass after the FK fix. Then update VERIFICATION.md status to passed and run `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" phase complete "01"`.
</next_action>
