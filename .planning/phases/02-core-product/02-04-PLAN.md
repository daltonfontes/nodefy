---
phase: 02-core-product
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/workspace/[id]/page.tsx
  - frontend/src/components/FirstPipelineForm.tsx
autonomous: false
gap_closure: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "Usuário autenticado abre /workspace/[id] e, se já existir ao menos um pipeline, é redirecionado para o board do primeiro pipeline"
    - "Usuário autenticado abre /workspace/[id] sem pipelines e vê um CTA funcional para criar o primeiro pipeline (admin) ou uma mensagem de espera (member)"
    - "Admin digita o nome do pipeline no formulário e, ao submeter, é levado ao board daquele pipeline recém-criado"
    - "Nenhum botão desabilitado com texto 'Disponível na próxima fase' permanece em /workspace/[id]"
  artifacts:
    - path: "frontend/src/app/workspace/[id]/page.tsx"
      provides: "Server Component que busca pipelines, redireciona para o primeiro existente ou renderiza o empty-state funcional"
      contains: "apiFetch"
    - path: "frontend/src/components/FirstPipelineForm.tsx"
      provides: "Client Component com input + botão que POSTa em /api/workspaces/[id]/pipelines e navega para o board criado"
      exports: ["FirstPipelineForm"]
  key_links:
    - from: "frontend/src/app/workspace/[id]/page.tsx"
      to: "/workspaces/{id}/pipelines (backend via apiFetch)"
      via: "apiFetch<Pipeline[]> com tenantId"
      pattern: "apiFetch.*workspaces.*pipelines"
    - from: "frontend/src/app/workspace/[id]/page.tsx"
      to: "/workspace/[id]/pipeline/[pipelineId]"
      via: "next/navigation redirect() quando pipelines.length > 0"
      pattern: "redirect.*pipeline"
    - from: "frontend/src/components/FirstPipelineForm.tsx"
      to: "/api/workspaces/[id]/pipelines (proxy route)"
      via: "fetch POST com { name }"
      pattern: "fetch.*workspaces.*pipelines.*POST"
    - from: "frontend/src/components/FirstPipelineForm.tsx"
      to: "/workspace/[id]/pipeline/[pipelineId]"
      via: "useRouter().push após sucesso do POST"
      pattern: "router\\.push.*pipeline"
---

<objective>
Fechar o gap identificado no UAT (teste 2): a rota `/workspace/[id]` é um stub da Phase 1 com botão desabilitado, sem nenhum caminho para criar o primeiro pipeline. O restante da UI da Phase 2 (`PipelineSidebar`, `BoardShell`, etc.) só aparece dentro de `/workspace/[id]/pipeline/[pipelineId]`, rota inacessível para um tenant novo.

Esta plan substitui o stub por uma página funcional que:
- Carrega a lista de pipelines do workspace no servidor (RSC).
- Redireciona direto para o board do primeiro pipeline quando já houver algum.
- Para admins sem pipelines, renderiza um formulário "Crie seu primeiro pipeline" que cria via POST existente e navega para o board do pipeline recém-criado.
- Para members sem pipelines, mostra uma mensagem explicando que um admin precisa criar o primeiro pipeline (consistente com a regra da `PipelineSidebar`, que só expõe o botão "+ Novo pipeline" para admins).

Purpose: Desbloqueia os 8 testes marcados como `blocked` no UAT (3–10 dependem de existir ao menos um pipeline).
Output: `page.tsx` reescrito como Server Component + novo `FirstPipelineForm.tsx` Client Component; botão desabilitado removido; fluxo end-to-end do teste 2 funciona.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product/02-UAT.md
@.planning/phases/02-core-product/02-03-SUMMARY.md
@frontend/src/app/workspace/[id]/page.tsx
@frontend/src/app/workspace/[id]/layout.tsx
@frontend/src/app/workspace/[id]/pipeline/[pipelineId]/page.tsx
@frontend/src/app/api/workspaces/[id]/pipelines/route.ts
@frontend/src/components/sidebar/PipelineSidebar.tsx
@frontend/src/hooks/use-pipelines.ts
@frontend/src/lib/api.ts
@frontend/src/types/api.ts

<interfaces>
<!-- Contratos já existentes que o executor deve consumir diretamente — não explorar o codebase. -->

From `frontend/src/types/api.ts`:
```typescript
export interface Pipeline {
  id: string
  name: string
  position: number
  createdAt: string
}

export interface Workspace {
  id: string
  name: string
  slug: string
  currency: "BRL" | "USD" | "EUR"
  currencyLocked: boolean
  role: "admin" | "member"
}
```

From `frontend/src/lib/api.ts`:
```typescript
// Server-only. Autentica via session, injeta Bearer token e X-Tenant-Id.
export async function apiFetch<T>(
  path: string,
  init?: RequestInit & { tenantId?: string }
): Promise<T>
export class ApiError extends Error {
  constructor(public status: number, public body: string)
}
```

From `frontend/src/app/api/workspaces/[id]/pipelines/route.ts`:
```typescript
// GET → Pipeline[] (já ordenado por position no backend)
// POST body { name: string } → Pipeline (status 201)
```

Padrão do layout do workspace (`frontend/src/app/workspace/[id]/layout.tsx`):
- Já chama `auth()` e redireciona para `/login?callbackUrl=...` quando sem sessão.
- Já busca `GET /workspaces` e redireciona para `/workspace/select` quando o id não bate.
- Já renderiza `WorkspaceTopNav` e envolve o `children` em `<main className="mx-auto max-w-6xl p-6">`.
- **Consequência:** a page NÃO precisa repetir auth-guard nem buscar a lista de workspaces, MAS precisa descobrir o `role` do workspace atual para decidir admin-vs-member. Faça uma segunda chamada `apiFetch<Workspace[]>("/workspaces")` e encontre o registro com `id === params.id` — é o mesmo padrão que o layout usa e mantém a page self-contained.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar FirstPipelineForm (Client Component)</name>
  <files>frontend/src/components/FirstPipelineForm.tsx</files>
  <behavior>
    - Recebe props `{ workspaceId: string }`.
    - Renderiza um `<form>` com um `<Input>` (name="name", required, maxLength 80, autoFocus) e um `<Button type="submit">Criar pipeline</Button>` usando os componentes de `@/components/ui/input` e `@/components/ui/button`.
    - No submit: faz `fetch('/api/workspaces/${workspaceId}/pipelines', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: trimmedName }) })`.
    - Valida `name.trim().length >= 1` antes de enviar; se vazio, não envia e exibe `toast.error('Informe um nome para o pipeline')` via `sonner`.
    - Durante o request: botão fica `disabled` e mostra texto "Criando...".
    - Em caso de sucesso (res.ok), lê `const pipeline = await res.json() as Pipeline` e chama `router.push(`/workspace/${workspaceId}/pipeline/${pipeline.id}`)` usando `useRouter()` de `next/navigation`.
    - Em caso de erro (!res.ok), lê o texto da resposta e exibe `toast.error(`Falha ao criar pipeline: ${texto}`)`. Re-habilita o botão.
    - Desativa reload do form: `onSubmit={(e) => { e.preventDefault(); handleSubmit() }}`.
    - Export nomeado `FirstPipelineForm`.
  </behavior>
  <action>
    Crie `frontend/src/components/FirstPipelineForm.tsx` com `"use client"` no topo. Importe `useState` de React, `useRouter` de `next/navigation`, `toast` de `sonner`, `Input` de `@/components/ui/input`, `Button` de `@/components/ui/button`, e `Pipeline` de `@/types/api`.

    Estrutura sugerida:
    ```tsx
    "use client"
    import { useState } from "react"
    import { useRouter } from "next/navigation"
    import { toast } from "sonner"
    import { Button } from "@/components/ui/button"
    import { Input } from "@/components/ui/input"
    import type { Pipeline } from "@/types/api"

    export function FirstPipelineForm({ workspaceId }: { workspaceId: string }) {
      const router = useRouter()
      const [name, setName] = useState("")
      const [submitting, setSubmitting] = useState(false)

      async function handleSubmit() {
        const trimmed = name.trim()
        if (!trimmed) {
          toast.error("Informe um nome para o pipeline")
          return
        }
        setSubmitting(true)
        try {
          const res = await fetch(`/api/workspaces/${workspaceId}/pipelines`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ name: trimmed }),
          })
          if (!res.ok) {
            const text = await res.text().catch(() => "")
            toast.error(`Falha ao criar pipeline: ${text || res.status}`)
            setSubmitting(false)
            return
          }
          const pipeline = (await res.json()) as Pipeline
          router.push(`/workspace/${workspaceId}/pipeline/${pipeline.id}`)
        } catch (err) {
          toast.error(`Falha ao criar pipeline: ${(err as Error).message}`)
          setSubmitting(false)
        }
      }

      return (
        <form
          className="flex flex-col gap-3"
          onSubmit={(e) => { e.preventDefault(); handleSubmit() }}
        >
          <Input
            name="name"
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="Ex: Vendas, Onboarding..."
            maxLength={80}
            autoFocus
            disabled={submitting}
            required
          />
          <Button type="submit" disabled={submitting}>
            {submitting ? "Criando..." : "Criar pipeline"}
          </Button>
        </form>
      )
    }
    ```

    Não usar o hook `usePipelines` aqui: essa página é empty-state, não precisa de cache TanStack Query — o fetch direto é mais simples e o redirect para o board vai refetch via `useQuery` do lado do board de qualquer forma. Evita também dependência circular de QueryClient num caminho quase estático.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    Arquivo `frontend/src/components/FirstPipelineForm.tsx` existe, exporta `FirstPipelineForm`, `npx tsc --noEmit` passa sem erros novos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever /workspace/[id]/page.tsx como Server Component funcional</name>
  <files>frontend/src/app/workspace/[id]/page.tsx</files>
  <behavior>
    - Server Component `async`, recebe `params: Promise<{ id: string }>`.
    - Faz `const { id } = await params`.
    - Em paralelo, busca:
      - `apiFetch<Pipeline[]>(`/workspaces/${id}/pipelines`, { tenantId: id })`
      - `apiFetch<Workspace[]>("/workspaces")` → encontra o workspace atual (`workspaces.find(w => w.id === id)`) para obter `role`.
    - Se o workspace não for encontrado, `redirect('/workspace/select')` (defensivo — o layout já faz isso, mas garante segurança caso o layout mude).
    - Se `pipelines.length > 0`:
      - Ordena por `position` ASC (o backend já retorna ordenado, mas garantir idempotência: `[...pipelines].sort((a,b) => a.position - b.position)`).
      - `redirect(`/workspace/${id}/pipeline/${pipelines[0].id}`)`.
    - Se `pipelines.length === 0`:
      - Se `role === 'admin'`: renderiza um `<Card>` contendo título "Comece criando seu primeiro pipeline", descrição curta, e `<FirstPipelineForm workspaceId={id} />`.
      - Se `role === 'member'`: renderiza um `<Card>` contendo título "Nenhum pipeline ainda" e texto "Peça ao admin deste workspace para criar o primeiro pipeline." (nenhum formulário).
    - **Proíbe**: texto "Disponível na próxima fase", `<Button disabled>` sem contexto de loading, stubs.
    - Mantém o wrapping em `<Card>`/`<CardContent>` para consistência visual com o stub atual, mas troca o conteúdo.
  </behavior>
  <action>
    Substitua COMPLETAMENTE o conteúdo de `frontend/src/app/workspace/[id]/page.tsx`.

    Imports necessários:
    - `redirect` de `next/navigation`
    - `apiFetch` de `@/lib/api`
    - Tipos `Pipeline`, `Workspace` de `@/types/api`
    - `Card`, `CardContent` de `@/components/ui/card`
    - `FirstPipelineForm` de `@/components/FirstPipelineForm`

    Não importar `auth()` nem `Button` — o layout já fez auth-guard, e o formulário vem do FirstPipelineForm.

    Estrutura:
    ```tsx
    import { redirect } from "next/navigation"
    import { apiFetch } from "@/lib/api"
    import type { Pipeline, Workspace } from "@/types/api"
    import { Card, CardContent } from "@/components/ui/card"
    import { FirstPipelineForm } from "@/components/FirstPipelineForm"

    export default async function WorkspaceHomePage({
      params,
    }: {
      params: Promise<{ id: string }>
    }) {
      const { id } = await params

      const [pipelines, workspaces] = await Promise.all([
        apiFetch<Pipeline[]>(`/workspaces/${id}/pipelines`, { tenantId: id }),
        apiFetch<Workspace[]>("/workspaces"),
      ])

      const workspace = workspaces.find((w) => w.id === id)
      if (!workspace) redirect("/workspace/select")

      if (pipelines.length > 0) {
        const first = [...pipelines].sort((a, b) => a.position - b.position)[0]
        redirect(`/workspace/${id}/pipeline/${first.id}`)
      }

      const isAdmin = workspace.role === "admin"

      return (
        <Card>
          <CardContent className="flex flex-col items-center gap-4 p-12 text-center">
            {isAdmin ? (
              <>
                <h2 className="text-xl font-semibold">Comece criando seu primeiro pipeline</h2>
                <p className="text-sm text-muted-foreground">
                  Pipelines organizam seus deals em estágios. Dê um nome para começar.
                </p>
                <div className="w-full max-w-sm">
                  <FirstPipelineForm workspaceId={id} />
                </div>
              </>
            ) : (
              <>
                <h2 className="text-xl font-semibold">Nenhum pipeline ainda</h2>
                <p className="text-sm text-muted-foreground">
                  Peça ao administrador deste workspace para criar o primeiro pipeline.
                </p>
              </>
            )}
          </CardContent>
        </Card>
      )
    }
    ```

    **Importante sobre `redirect()` do Next 16**: `redirect()` lança uma exceção interna para abortar a render — NÃO usar dentro de try/catch que engole `NEXT_REDIRECT`. No código acima o redirect está fora de try/catch, então está seguro.

    **Nota sobre double-fetch do Workspace[]**: o layout também chama `apiFetch<Workspace[]>("/workspaces")`. O Next 16 deduplica `fetch` do mesmo request por padrão quando usa `cache: "no-store"` + mesmo URL e headers dentro de uma mesma árvore RSC — `apiFetch` já usa `cache: "no-store"` mas as chaves de request podem divergir pelo token minted. Isso é um custo aceitável (duas chamadas rápidas ao backend) e mantém a página self-contained. **Não tente** expor o workspace via contexto/prop do layout — isso quebra o padrão estabelecido pelos outros routes (pipeline page também re-busca).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` passa sem erros novos.
    - `npm run build` completa sem erros no route `/workspace/[id]/page`.
    - Leitura manual do arquivo confirma: sem "Disponível na próxima fase", sem `<Button disabled>`, import de `FirstPipelineForm` presente, `redirect` para pipeline quando `pipelines.length > 0`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — re-run UAT test 2 and unblock 3–10</name>
  <files>(verification only)</files>
  <action>(see how-to-verify — human runs scenarios A/B/C/D against the live stack)</action>
  <what-built>
    - Empty-state funcional em `/workspace/[id]` para admins sem pipelines (formulário).
    - Empty-state informativo para members sem pipelines.
    - Redirect automático para o board do primeiro pipeline quando já existirem pipelines.
    - Remoção do stub "Disponível na próxima fase".
  </what-built>
  <how-to-verify>
    Repita os testes que estavam bloqueados no 02-UAT.md. Suba o stack (`docker compose up` ou o fluxo normal de dev) e faça login.

    **Cenário A — Tenant novo sem pipelines (admin):**
    1. Crie um novo workspace OU acesse um que não tenha pipelines.
    2. Você deve ser levado para `/workspace/[id]` e ver o card "Comece criando seu primeiro pipeline" com um input e um botão "Criar pipeline".
    3. Deixe o campo vazio e clique em "Criar pipeline" — deve aparecer um toast vermelho "Informe um nome para o pipeline" e NADA é criado.
    4. Digite "Vendas" e clique em "Criar pipeline". O botão mostra "Criando...", depois a URL muda para `/workspace/[id]/pipeline/[novo-pipeline-id]` e o board aparece com a `PipelineSidebar` mostrando "Vendas" selecionado.
    5. A partir daqui, prossiga com os testes 3–10 do 02-UAT.md (criar stage, criar card, drag, etc.) — todos devem desbloquear.

    **Cenário B — Tenant já com pipelines:**
    1. Acesse `/workspace/[id]` de um workspace que já tem pelo menos um pipeline.
    2. Você deve ser redirecionado AUTOMATICAMENTE para `/workspace/[id]/pipeline/[firstPipelineId]` (não ver o empty-state).

    **Cenário C — Member sem pipelines (opcional se não houver member disponível):**
    1. Convide um segundo usuário como `member` para um workspace sem pipelines.
    2. Logado como esse member, acesse `/workspace/[id]`.
    3. Deve ver "Nenhum pipeline ainda" com texto pedindo para contatar o admin — SEM formulário.

    **Cenário D — Regressão:**
    1. Depois de criar o primeiro pipeline, recarregue `/workspace/[id]` (sem o `/pipeline/[id]` no path). Deve redirecionar direto para o board.
    2. Verifique que os testes 1 (cold start) e 2 (criar pipeline) do UAT agora estão `pass`.

    Atualize `.planning/phases/02-core-product/02-UAT.md` marcando os testes re-executados com seus novos resultados.
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification only; automated coverage owned by Phase 4 Playwright suite (TEST-02)</automated>
  </verify>
  <done>
    Cenário A completa com sucesso (pipeline criado e redirect para o board). UAT atualizado com testes 2 re-executado (pass) e 3–10 desbloqueados.
  </done>
  <resume-signal>Digite "approved" quando o cenário A completar com sucesso (pipeline criado + redirect para o board) ou descreva o problema para abrirmos outro ciclo de fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next RSC | Usuário autenticado atinge o Server Component; já protegido pelo layout via `auth()`. |
| Next RSC → backend .NET | `apiFetch` injeta JWT + X-Tenant-Id; backend valida tenant ownership via RLS + global query filter. |
| browser → Next API proxy (`/api/workspaces/[id]/pipelines`) | Client Component POSTa; proxy valida sessão via `apiFetch` antes de encaminhar. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Spoofing | `/workspace/[id]/page.tsx` acessado sem sessão | mitigate | Layout pai (`workspace/[id]/layout.tsx`) já chama `auth()` e redireciona para `/login?callbackUrl=...`. Page não precisa duplicar. |
| T-02-04-02 | Tampering | Usuário força `workspaceId` de outro tenant na URL | mitigate | `apiFetch` envia JWT com `sub` do usuário + `X-Tenant-Id`; backend retorna 403/vazio via RLS + global query filter; `workspaces.find(w => w.id === id)` falha e redirect para `/workspace/select`. |
| T-02-04-03 | Elevation of Privilege | Member tenta forçar POST `/api/workspaces/[id]/pipelines` mesmo sem o UI expor o form | accept | Autorização é feita no backend (`PipelineEndpoints.cs` exige admin). Ocultar o form para member é UX, não defesa — defesa real está no backend onde já foi validada na Phase 2. |
| T-02-04-04 | Information Disclosure | Nome de pipeline contém payload XSS renderizado no form | mitigate | Formulário só manipula `name` como string; redirect pós-criação navega por ID, não por nome. O `router.push` usa apenas `pipeline.id`, que é UUID gerado pelo backend, não input do usuário. |
| T-02-04-05 | Denial of Service | Submit em rápida sucessão (click-flood) dispara múltiplas criações | mitigate | Botão fica `disabled={submitting}` durante o POST; em caso de erro o estado volta para idle. Rate limiting global fica com o backend. |
</threat_model>

<verification>
- Rodar `npx tsc --noEmit` em `frontend/` — deve passar sem erros novos.
- Rodar `npm run build` em `frontend/` — a rota `/workspace/[id]` deve compilar como dynamic server route.
- Re-executar os cenários do 02-UAT.md teste 2 (e desbloquear 3–10) conforme `<how-to-verify>` do checkpoint.
- Confirmar via `grep -n "Disponível na próxima fase" frontend/src/` — não deve retornar nada.
</verification>

<success_criteria>
- Teste 2 do 02-UAT.md passa com resultado `pass` (usuário admin novo cria pipeline a partir de `/workspace/[id]` e aterrissa no board).
- Testes 3–10 do 02-UAT.md deixam de estar `blocked_by: prior-phase` — ficam disponíveis para re-execução (não precisam passar nesta plan, apenas desbloquear).
- Nenhuma regressão em `/workspace/[id]/pipeline/[pipelineId]` (board continua funcionando para tenants que já tinham pipeline).
- Nenhum arquivo fora dos listados em `files_modified` é alterado.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-product/02-04-SUMMARY.md` seguindo o template padrão. O summary deve documentar:
- Que o gap de teste 2 foi fechado.
- Decisão de usar fetch direto no Client Component em vez de `usePipelines` (justificativa: empty-state não precisa de cache TanStack Query).
- Decisão de deixar o layout responsável pelo auth-guard e a page responsável por descobrir `role` via segunda chamada `GET /workspaces`.
- Confirmação de que os testes 3–10 ficaram disponíveis para re-execução.
</output>
